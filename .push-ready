Echo accreditation only — lung ultrasound removed, 18 Sept.

Ali, on seeing it: "lung US not required really, remove that but otherwise agree to how
displayed" and "dont need to say declared btw".

Lung is gone entirely rather than hidden — the FLAGS entry, the icon, the colour, the staff
column and the Who can… key. A test asserts the string `lungus` appears nowhere in the page,
so it cannot come back by halves. Only four people still here held it and nobody was asking
the question.

The portal view is rebuilt to FUSIC Heart alone and carries no route column, so the board
cannot distinguish self-declared from portal-assessed even if somebody later wanted it to.
Sixteen people.

What is left is one find skill, exactly like PICC: searchable from Who can…, read by nothing
in the allocator, granted by the nightly sync and never revoked by it.

  index.html                                                FLAGS, icon, colour, SK_COLS, FIND_KEYS
  tests/render-tests.js                                     eight assertions, echo only
  docs/CCU Pod Allocator/allocate-pull/ultrasound_sync.py    name + one boolean

Tests: render 311 passed / 1 failed (pre-existing, unrelated) · locum probe 27/0.
